# Daily report and housekeeping - 2026-08-18

## Headline

eliandoran merged all three of our open upstream PRs today:

| PR | Title | Merged (UTC) |
|---|---|---|
| TriliumNext/Trilium 11063 | fix(text editor): anchor reference-link insertion at mention pick position | 17:51 |
| TriliumNext/Trilium 11062 | fix(client/tooltips): put away delegated tooltip whose trigger left the DOM | 18:18 |
| TriliumNext/Trilium 11064 | docs: add CONTRIBUTING.md | 19:32 |

The four gated replies to eliandoran's review comments on 11064 were posted
2026-08-17 21:04 after human review. The comment gate worked end to end:
draft, lint, human approval, post, merge.

## Caveat on 11063

eliandoran's merge comment: the fix is good but it does not fix the issue
reported in 10663 - it only fixes cursor mis-positioning, not the duplicated
text. GitHub auto-closed issue 10663 on merge anyway (one minute before his
comment). Follow-up tracked in bead mitn-5kv.15.

## Bead changes

- Closed `mitn-znf` (raise codecov patch coverage on PR 11062): maintainer
  merged with codecov/patch red at 70.37%; acceptance criterion is moot.
- Created `mitn-5kv.15` (P2, child of epic): 10663 duplicated-text symptom
  remains after PR 11063; reproduce, root-cause, draft gated comment.
- Created `mitn-0ye` (P3): AGENTS.md session-close step cites
  `bd doctor --check=...`, which bd 1.2.2 does not support in embedded mode.
- Created `mitn-jza` (P3, maphew): decide fate of the 6-hourly cloud PR
  babysitter routine - all watched PRs are merged, it now burns quota idle.
- Epic `mitn-5kv` stays in progress: acceptance still needs the comment-gate
  retrospective decision (mitn-bws) and learnings folded into
  docs/trilium-landscape-2026-08.md.

## Housekeeping done

- Fresh WSL environment: cloned maphew/mitn into ~/dev/mitn and restored
  Beads state from refs/dolt/data via scripts/bootstrap-beads.
- bd schema incident recurrence: the stale bd 1.2.1 on PATH auto-migrated the
  freshly bootstrapped local db to v65 (remote stayed safe at v53 behind the
  skew guard). After maphew upgraded to bd 1.2.2, recovered by wiping the
  local runtime state and re-bootstrapping. Memory
  `beads-121-schema-recovery` updated with this fresh-clone recovery path.
- `git config beads.role maintainer` set; `.beads` perms tightened to 700.
- Hygiene pass: main checkout clean and up to date with origin; no worktrees,
  no stashes, no stray branches; only ignored Beads runtime files untracked.
- `bd dolt push` and git push verified in sync at session close.

## Noticed, not on any list

- The sibling `../trilium` source checkout does not exist on this machine.
  AGENTS.md assumes it. Next source-level task here must clone
  maphew/Trilium to ~/dev/trilium first (mitn-5kv.15 will need it).
- The cloud babysitter routine's clone may still run bd 1.2.1, which would
  re-migrate the db to v65 on its next run; flagged inside mitn-jza.

## Ready queue after today

mitn-5kv.15 (10663 follow-up), mitn-9l3 (rubric ambiguities with Elian),
mitn-bws (comment-gate retrospective - now has strong evidence: 3 merges,
zero gate violations), mitn-0ye, mitn-jza, mitn-3ac.
