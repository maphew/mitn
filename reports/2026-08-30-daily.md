# Daily report and housekeeping - 2026-08-30

Covers 2026-08-22 through 2026-08-30 (no daily reports in between; most
of the work ran in Amp orbs and is recorded in closed beads).

## Headline

A busy nine days upstream, driven from Amp orbs: PR 11210 (Markdown
preview style leak, the revival of PR 9633) merged on 2026-08-27 with
eliandoran's approval; two new PRs opened (11147 fork-guard CI, 11133
Trillium spelling) and both are green but unreviewed by maintainers; and
a large review sweep posted 55 signed reviews on open outsider PRs (12
across the two "six oldest" rounds, 43 from the 08-29 audit). Today was
housekeeping: the shared `../trilium` checkout was 843 commits behind
upstream with six finished worktrees still on disk, and one closed bead
recorded deliverables that never reached this repository.

## Upstream state

- PR 11147 (fork-guard CI): head 3296d242d3 after a merge of upstream
  main on 08-29, 22/22 checks green, mergeable. Reviews are Greptile
  (08-22) and our own amp self-review (08-29) only. Issue 11146 has no
  comments. Opened 08-22; no ping before two weeks of silence.
- PR 11133 (Trillium spelling): 24/24 checks green, same self-review
  pattern, no maintainer reply.
- PR 9633: closed by eliandoran 08-27 in favor of our PR 11210. The
  original author (AllanZyne) had said they lacked time and invited a
  replacement PR, so the outcome matches their wish.
- Rescue offers (mitn-duu): PR 8980's author took the rescue branch,
  re-rebased it, and kept the repair commit's authorship. PR 8894's
  author declined the import cleanup on purpose (keeps the PR out of
  conflicts) and will cherry-pick the test. No action for us.
- PR 9508: author replied 08-27 declining two style suggestions with
  reasons (max-len is not enforced in CI; file already mixes styles).
  Their reasoning holds; no follow-up needed.
- PR 10824 (plugin manager): iansherr replied 08-24 with a fork, a
  planning document, and more queued PRs; maphew answered in person
  clarifying the contributor role. Human conversation, not agent work.
- Of the 51 open PRs maphew has reviewed, only 9508, 8980, and 8894
  have activity by others after our review. The rest are quiet.
- Fork main (`maphew/Trilium`) carries one commit upstream does not:
  cf1e97cc89, fork PR 4, merged by maphew 08-22, guarding main-docker
  publish jobs. Recorded in memory `fork-ci-policy` as an exception;
  reconcile when PR 11147 lands (mitn-2dq).

## Bead changes

- Created `mitn-cmk`: recover the 2026-08-29 unreviewed-PR likelihood
  report. `mitn-dm6` closed on 08-29 citing
  `reports/2026-08-29-unreviewed-prs.md` and 43 draft files under
  `drafts/unreviewed-prs-2026-08-29/`; neither exists in the checkout,
  on `origin/main`, or on any mitn branch. The orb posted the reviews
  (all 43 verified on GitHub) but never committed the report. `mitn-b9b`
  now depends on `mitn-cmk`.
- `mitn-b9b` (rubric triage, in progress since 08-17): noted the overlap
  with `mitn-dm6` and the remaining scope (rubric lanes for the
  unreviewed remainder; LLM lane still gated on `mitn-9l3`).
- `mitn-5kv` epic: 16/16 children closed, but its acceptance criteria
  need the `mitn-bws` comment-gate decision. bd refuses epic-depends-on-
  task edges, so `mitn-bws` is now a child of the epic instead.
- `mitn-6ff`: PR 11147 status noted; waiting only.
- `mitn-jza`: the "zero open PRs" premise for retiring the babysitter
  routine no longer holds; two PRs are open again.
- Memories updated: `fork-ci-upstream-fix` (new head SHA, review state),
  `contributing-guide-in-effect` (points at upstream main now that the
  worktree copy is gone), `fork-ci-policy` (fork PR 4 exception).

## Housekeeping done

- Health checks before edits: `bd lint`, `bd stale`, `bd orphans` all
  clean; `git status --porcelain .beads/` empty; `bd dolt pull` brought
  nothing new. Rerun after edits, results in the commit that carries this
  report.
- `../trilium` main fast-forwarded from 372a749ff2 to cf1e97cc89
  (`origin/main`). It is still one merge behind `upstream/main` because
  the fork's main sync lags; topic branches are cut from `upstream/main`
  anyway.
- Removed six clean worktrees under `.worktrees/trilium/` (about 12 GB):
  `contributing-md`, `fix-10663`, `fix-10680` (all three PRs merged in
  August) and `pr10371-oidc`, `pr10633-search`, `pr10825` (review
  beads closed 08-17; two of the three were already stale against the
  PR heads). Each had zero dirty files and zero unpushed commits.
- Deleted four merged local branches: `fix/10663-anchor-reference-link`,
  `fix/10680-delegated-tooltip-orphan`, `docs/contributing-guide`,
  `fix-9749-mermaid-resize`. Their fork copies are untouched.
- Kept: worktree `fork-workflow-guards` (fast-forwarded 20 commits to
  match the fork branch, PR 11147 still open), local branch
  `fix/trillium-discovery-11122` (reset to its fork copy), and local
  review branches `pr10371-oidc-refresh` and `pr10633-search` (cheap,
  no worktree).
- Fork remote branches for merged PRs (`fix/10663-...`, `fix/10680-...`,
  `docs/contributing-guide`, `fix-9749-mermaid-resize`,
  `fix/markdown-preview-style-leak`) are still on `maphew/Trilium`.
  Left alone; the 08-21 report said they were pruned, which was wrong.
- mitn branch `origin/chore/orb-setup` is fully merged into main; left
  in place, safe to delete.

## Noticed, not on any list

- Amp orb sessions close beads with deliverables described in the close
  reason but not always committed. `mitn-dm6` is the first confirmed
  case. `mitn-cmk` carries the fix (add "commit before closing" to the
  orb close procedure), but the same check is worth running on every
  orb-closed bead in future dailies.
- The two open PRs each got a self-review from an Amp agent on 08-29
  ("No technical findings", signed amp-gpt-5.6-sol-high). Harmless, but
  a review of one's own PR reads oddly to maintainers; advise keeping
  cross-vendor checks in the PR body or in mitn drafts instead.
- The `unslop` skill named in memory `unslop-all-prose` is not available
  in this Claude Code session; this report was written plainly and
  passed through `scripts/gh-body-lint` as the available substitute.

## Ready queue after today

mitn-0kt.2 (off-site backup, gates the rest of the mhwcom epic),
mitn-0kt.4 (ETAPI export), mitn-9l3 (rubric ambiguities with Elian),
mitn-bws (comment-gate decision, now also gates closing mitn-5kv),
mitn-csd (float lint check to dotfiles), mitn-jza (babysitter decision,
premise changed), mitn-cmk (recover the 08-29 likelihood report).
