# Fix batch 2026-09-16 ledger

| key | stage | status | triage confidence | reason |
|---|---|---|---|---|
| 10456 | triage | no-go | medium | This is a real defect in 0.103.0, but upstream/main already fixed it. Commit bd67fa7008 (closes issue 10989, shipped in v0.105.0) fixed it, and 8e9ce16b58 and d1dda881ed followed up. Issue 10456 has the same symptom and the same cause as 10989: a pasted custom HTML element shows in read mode and is invisible in edit mode. No code change remains. The useful output is a comment that points the reporter to the fix and asks for a retest on 0.105.0 or later. |
| 10588 | triage | no-go | high | Issue 10588 is a real defect and is still present on upstream/main, but open PR 11235 by jayhormes already fixes it and says "Fixes #10588" in its body. That PR is mergeable, not a draft, its CI is green, and it has an approving review from maphew. A second PR would duplicate it and would conflict in share_link.ts. |
| 10705 | final | ready | medium | Issue 10705 is an umbrella of several independent bugs. One of them (bucket A: a Bootstrap tooltip stays on top of a context menu after right-click on a launcher icon or the Web clip badge) was reproduced live on 2026-08-17. The code behind it is unchanged on upstream/main 1a2f27f8d3. Nobody is assigned and no PR overlaps. The fix is about 10 lines in the context menu owner, with a feasible happy-dom regression test. PR 11062 fixed only the delegated-tooltip orphan (issue 10680), not this path. The PR will fix part of 10705, not all of it, so it must not claim to close the issue. |
| 11192 | triage | no-go | high | This is a real defect in v0.104.0, but upstream/main already fixed it. Commit 5e02f7d19b (2026-08-05, shipped in v0.105.0) removed the app-wide `.popover { position: fixed !important }` rule that caused it. The desktop repro video shows the "Get Version 0.105.0" update banner, so it was recorded on v0.104.0. No code change remains to make, only a comment on the thread asking for a retest on 0.105.0 or later. |
| 11312 | triage | no-go | medium | No-go: I found no defect in the source, so there is nothing small and testable to fix. The shipped help tree at v0.105.0 and on upstream/main has no children under "Premium features", and the code that moves relocated help notes is present and already covered by a test. The symptom is most likely caused by the reporter's environment: a sync peer still on 0.104.x, or a hidden-subtree check that failed and rolled back. A change that stops mixed-version peers from overwriting each other's help notes would be a design decision, not a small bug fix. |
| 11322 | final | agent-failed | medium | This is a real defect: an uncaught InvalidStateError escapes the pan/zoom setup effect of the Mermaid preview, and the stack maps exactly to one line on upstream/main. No PR, assignee or maintainer direction overlaps. The fix is about 15 lines in one hook and follows the maintainer's own three earlier guards for this error. It can be unit-tested in the existing spec. The reporter's exact steps are unknown, so confidence is medium. |
| 11322 | final | ready | medium | This is a real defect: an uncaught InvalidStateError escapes the pan/zoom setup effect of the Mermaid preview, and the stack maps exactly to one line on upstream/main. No PR, assignee or maintainer direction overlaps. The fix is about 15 lines in one hook and follows the maintainer's own three earlier guards for this error. It can be unit-tested in the existing spec. The reporter's exact steps are unknown, so confidence is medium. |
| 11403 | triage | no-go | medium | The crash is inside CKEditor 5's own "Powered by" badge positioning code, not in Trilium code. No Trilium file is in the stack. The trigger cannot be found from code: with non-negative rect sizes the failing intersection cannot be null (checked by reading the code and by a 2M-sample fuzz). The report gives no reproduction steps. A Trilium-side fix would be a workaround (a pnpm patch of ckeditor5-ui, or a change to how the badge is suppressed). That is a design and licence-sensitive decision, and no honest regression test is possible without a known trigger. |
| 11447 | final | agent-failed | high | This is a clear regression from the Preact port of the promoted attributes grid. Every promoted field gets a positive tabindex (200 + position) except the single-valued relation field, so the browser takes that field out of the Tab sequence. The defect is still present on upstream/main at 1a2f27f8d3, and there is no assignee, maintainer comment or overlapping fix. The fix is about 10 changed lines, and a regression test is feasible in the existing happy-dom specs. |
| 11447 | final | not-ready | high | This is a clear regression from the Preact port of the promoted attributes grid. Every promoted field gets a positive tabindex (200 + position) except the single-valued relation field, so the browser takes that field out of the Tab sequence. The defect is still present on upstream/main at 1a2f27f8d3, and there is no assignee, maintainer comment or overlapping fix. The fix is about 10 changed lines, and a regression test is feasible in the existing happy-dom specs. |
| 11458 | final | ready | medium | This is an omission from the maintainer's own feature (PR 10151, commit 64fef95f19). That PR states that tabs opened by ctrl-click, middle-click, or the "Open in a new tab" context menu go right after the active tab. The launch-bar launcher and the shared link context menu never received the `placement: "afterCurrent"` option, so a bookmark opens at the end while a `#bookmarkFolder` entry in the same widget opens after the active tab. The defect is still on upstream/main at 1a2f27f8d3. There is no open PR, no assignee, and no maintainer comment. The fix is about 5 changed lines plus specs. Residual risk: browsers open bookmark-bar tabs at the end, so a maintainer could prefer that. The in-app inconsistency and the PR text support treating it as a bug. |
| 11459 | final | agent-failed | medium | This is a cosmetic layout defect, not a feature request. The reference-link icon is an inline-block, so the browser can wrap the line between the icon and the first word of the title. It is still present on upstream/main at 1a2f27f8d3, with no assignee, no maintainer comment and no overlapping PR. I reproduced the orphaned icon in headless Chromium with a minimal model of Trilium's markup and confirmed a roughly 12-line pure-CSS fix there. A pure-CSS fix is exempt from the coverage gate. |
| 11459 | final | not-ready | medium | This is a cosmetic layout defect, not a feature request. The reference-link icon is an inline-block, so the browser can wrap the line between the icon and the first word of the title. It is still present on upstream/main at 1a2f27f8d3, with no assignee, no maintainer comment and no overlapping PR. I reproduced the orphaned icon in headless Chromium with a minimal model of Trilium's markup and confirmed a roughly 12-line pure-CSS fix there. A pure-CSS fix is exempt from the coverage gate. |
| 11472 | final | agent-failed | medium | Issue 11472 is a real desktop defect, still on upstream/main at 1a2f27f8d3. The note-actions dropdown has no vertical overflow guard, and its "Advanced" submenu never flips upward. The maintainers already fixed both problems elsewhere: IconPicker (popper `altAxis`) and ContextMenu (`repositionSubmenu`). The fix reuses those two patterns, stays well under 150 lines, and is unit-testable. No PR or assignee overlaps. The hint that one fix closes both issues is wrong. Issue 11106 has a different cause: mobile CSS scoped to `max-width: 991px`. The maintainer eliandoran replied "I will check" and is considering removing tablet mode, so keep 11106 out of this PR. Residual limit: a window shorter than the whole menu (estimated at about 640px from the issue screenshot) still clips. The main menu cannot scroll without clipping its submenus. That case is a separate design matter. |
| 11472 | final | not-ready | medium | Issue 11472 is a real desktop defect, still on upstream/main at 1a2f27f8d3. The note-actions dropdown has no vertical overflow guard, and its "Advanced" submenu never flips upward. The maintainers already fixed both problems elsewhere: IconPicker (popper `altAxis`) and ContextMenu (`repositionSubmenu`). The fix reuses those two patterns, stays well under 150 lines, and is unit-testable. No PR or assignee overlaps. The hint that one fix closes both issues is wrong. Issue 11106 has a different cause: mobile CSS scoped to `max-width: 991px`. The maintainer eliandoran replied "I will check" and is considering removing tablet mode, so keep 11106 out of this PR. Residual limit: a window shorter than the whole menu (estimated at about 640px from the issue screenshot) still clips. The main menu cannot scroll without clipping its submenus. That case is a separate design matter. |
| 1qa | final | agent-failed | medium | This is a real defect in the shared Dropdown component, still present on upstream/main 1a2f27f8d3, with no overlapping PR, issue, assignee, or maintainer statement. The root cause is certain from Bootstrap 5.3.8 source. The fix is about 10 lines in one file and a regression test fits the existing spec files. Two caveats. First, user impact is low: the bead is P3, there is no user report, and the production paths are narrow. Second, the most likely production path, the LLM chat model selector, comes from reading code only. It has not been reproduced in a browser. |
| 1qa | final | ready | medium | This is a real defect in the shared Dropdown component, still present on upstream/main 1a2f27f8d3, with no overlapping PR, issue, assignee, or maintainer statement. The root cause is certain from Bootstrap 5.3.8 source. The fix is about 10 lines in one file and a regression test fits the existing spec files. Two caveats. First, user impact is low: the bead is P3, there is no user report, and the production paths are narrow. Second, the most likely production path, the LLM chat model selector, comes from reading code only. It has not been reproduced in a browser. |
| 6474 | final | agent-failed | medium | Issue 6474 is a defect and is still present on upstream/main (1a2f27f8d3); the newer issue 10820 describes the same defect. The root cause was found by reading the code. The cause fits both reports and an earlier maintainer fix for the same class of bug on the columns side (00bff43d63). No PR, assignee or maintainer direction overlaps. The fix is about 30 lines in one file and can be unit-tested with a mocked Tabulator. Nothing was run at runtime in this read-only stage, so the first build step must be a manual reproduction. |
| 6474 | final | ready | medium | Issue 6474 is a defect and is still present on upstream/main (1a2f27f8d3); the newer issue 10820 describes the same defect. The root cause was found by reading the code. The cause fits both reports and an earlier maintainer fix for the same class of bug on the columns side (00bff43d63). No PR, assignee or maintainer direction overlaps. The fix is about 30 lines in one file and can be unit-tested with a mocked Tabulator. Nothing was run at runtime in this read-only stage, so the first build step must be a manual reproduction. |
| 6853 | final | ready | medium | Confirmed defect on upstream/main 1a2f27f8d3: a comma typed in a definition's alias is cut by the tokenizer, so the alias is truncated and the fragments can run as live tokens (alias "Size, number" sets the label type; "Foo, promoted" promotes). No PR, no assignee, no maintainer direction. The fix is small, lives in one pure function pair, and follows the maintainer's own July 2026 precedent in the same file (encodeOption/decodeOption percent-substitution for select options). Risk: the issue offers "forbid or escape" and the maintainer has not chosen; the precedent makes escape the safer guess. |
| 7085 | triage | no-go | medium | The root cause cannot be established from code. On upstream/main (1a2f27f8d3), quick search, jump-to autocomplete and full search all exclude archived notes by default. An existing unit test already proves the exclusion through the progressive/fuzzy path. The report has no reproduction steps, and the same filter was present in v0.97.2 and v0.98.0. Without a reproduction, a PR would be a guess or a redundant filter, as in the closed draft PR 7222. |
| 7288 | final | agent-failed | medium | This is a real UI defect and it is still present on upstream/main (1a2f27f8d3). The issue has no assignee, no maintainer direction, and no overlapping PR. I did not reproduce it, but the root cause follows from the code and fits both error dumps in the thread: each ghost branch is the one moved just before a `GET /api/tree?subTreeNoteId=<target>`. The fix is about 5 lines in one client service, and a deterministic unit test fits the existing `froca_updater.spec.ts`. |
| 7288 | final | ready | medium | This is a real UI defect and it is still present on upstream/main (1a2f27f8d3). The issue has no assignee, no maintainer direction, and no overlapping PR. I did not reproduce it, but the root cause follows from the code and fits both error dumps in the thread: each ghost branch is the one moved just before a `GET /api/tree?subTreeNoteId=<target>`. The fix is about 5 lines in one client service, and a deterministic unit test fits the existing `froca_updater.spec.ts`. |
| 7996 | final | agent-failed | medium | Real defect (typed characters corrupt the note), still present on upstream/main 1a2f27f8d3, no assignee, no overlapping PR, no maintainer direction (label State: Triage; only a bot and two users commented). Root cause is clear from code plus Bootstrap 5.3.8 source, and a 5-10 line owner-level fix in openDialog is unit-testable in the existing dialog.spec.ts. Caveat: the small fix drops the early keystrokes instead of routing them into the dialog input (the reporter names this as acceptable); a maintainer could ask for full type-ahead, which is a larger per-dialog change. |
| 7996 | final | ready | medium | Real defect (typed characters corrupt the note), still present on upstream/main 1a2f27f8d3, no assignee, no overlapping PR, no maintainer direction (label State: Triage; only a bot and two users commented). Root cause is clear from code plus Bootstrap 5.3.8 source, and a 5-10 line owner-level fix in openDialog is unit-testable in the existing dialog.spec.ts. Caveat: the small fix drops the early keystrokes instead of routing them into the dialog input (the reporter names this as acceptable); a maintainer could ask for full type-ahead, which is a larger per-dialog change. |
| 8169 | final | agent-failed | medium | Core createNewNote() accepts any forced noteId, while every link parser (client and server) only matches [A-Za-z0-9_], so a dashed GUID id silently breaks links and internalLink relations. ETAPI already rejects such ids with isValidEntityId, so the rule exists and the script API / internal API path simply lacks it; the fix is a ~10 line guard in one core function with an easy real-DB spec. No PR, assignee, or maintainer direction exists; the only caveat is that no maintainer has confirmed the issue (label still "State: Triage") and the change makes previously "working" scripts that force dashed ids throw. |
| 8169 | final | ready | medium | Core createNewNote() accepts any forced noteId, while every link parser (client and server) only matches [A-Za-z0-9_], so a dashed GUID id silently breaks links and internalLink relations. ETAPI already rejects such ids with isValidEntityId, so the rule exists and the script API / internal API path simply lacks it; the fix is a ~10 line guard in one core function with an easy real-DB spec. No PR, assignee, or maintainer direction exists; the only caveat is that no maintainer has confirmed the issue (label still "State: Triage") and the change makes previously "working" scripts that force dashed ids throw. |
| 8331 | triage | no-go | medium | The defect is real but upstream/main already fixed its root cause: commit 43017a0ebe (2026-08-11, "fix(splits): detach a closed split from the widget tree") plus 5db8eb84c4 (unmount the Preact tree on cleanup), both shipped in v0.105.0 and both covered by specs. No code change remains for a PR; the only useful action is a maintainer-reviewed note on the issue asking the reporter to retest on 0.105.0. Not verified at runtime (read-only stage), so the conclusion is from code reading only. |
| 8448 | final | ready | medium | The reporter's headline expectation (shared page auto-redirects or embeds the URL) is by design and documented, and a PR must not claim to deliver that. But the thread exposes a real, still-present defect next to it: the left navigation tree of the share theme ignores #shareExternalLink (it reads only the legacy #shareExternal), while the in-app attribute help says the note "will act as a link to an external website in the share tree" and Sharing.md names "the shared navigation" as a use. The fix is about 5 lines in one EJS template, a test harness for that template already exists, nobody is assigned, and no PR overlaps. Caveat: the PR should say it fixes the tree part of 8448 ("Refs", not "Closes"), because the reporter's single-note repro has no tree link at all. |
| 8576 | triage | no-go | high | The defect was real in 0.101.1, but merged PR 8431 (released in v0.102.0) already fixes it on upstream/main. That PR includes a unit test for the reporter's exact case: 2026-02-01 with a Sunday week start gives 2026-W06. Maintainers eliandoran and JYC333 both said in the thread that PR 8431 fixes this issue. The issue is only still open because nobody closed it, so there is nothing left to patch. |
| 8942 | final | ready | high | Clear regression with a code-level root cause: the share renderer rewrites a reference link's href to `./<shareId>` and then re-parses that rewritten href as a note ID, so any target with a #shareAlias is "not found" and the anchor is replaced by plain text. Still present on upstream/main 1a2f27f8d3, no assignee, no open PR, no maintainer direction; fix is about 15 lines in one file and the existing content_renderer.spec.ts harness already supports #shareAlias mocks. |
| 9083 | triage | no-go | medium | No code path on upstream/main (1a2f27f8d3) or at v0.102.1 closes all windows when one window closes, so the cause is not established from code. The symptom ("stuck for a few seconds, then all close") fits a main-process stall or crash on Windows more than deliberate logic. This host is Linux, so the Windows 11 report cannot be reproduced here. Without a known cause there is no fix spec and no regression test that would fail before a fix. The reporter also calls the behaviour possibly intended and asks for VS Code-like behaviour. The issue is still labelled "State: Triage" and has no maintainer reply. |
| 9549 | triage | no-go | medium | The cause of the mismatch is established. The edit-mode TOC skips a pasted H1 and the read-only TOC lists it. Which side is wrong is a design decision: H1 is reserved for the note title, and no maintainer has stated a direction. The report itself is also ambiguous about which behaviour is the malfunction. On upstream/main General HTML Support has been opt-in and off by default since 8e9ce16b58 (2026-08-12), and single-file import already shifts H1 headings down. From code reading (not run), the reported paste scenario should therefore no longer occur with default settings. |
| 9753 | final | agent-failed | medium | Real defect with a code-level root cause: Trilium never sets an Electron application menu, so Electron's default menu (hidden, but accelerators live) minimizes the window on Ctrl+M whenever the renderer does not consume the key; this collides with Trilium's own Insert Math Ctrl+M. No assignee, no PR, no maintainer direction (only dosubot comments, all of which guess wrong). Fix is ~15 lines in one file plus a spec in an existing, well-mocked spec file. Not reproduced live (read-only stage); one small design choice remains (trimmed menu vs null menu), so I chose the variant with the least behaviour change. |
| 9753 | final | not-ready | medium | Real defect with a code-level root cause: Trilium never sets an Electron application menu, so Electron's default menu (hidden, but accelerators live) minimizes the window on Ctrl+M whenever the renderer does not consume the key; this collides with Trilium's own Insert Math Ctrl+M. No assignee, no PR, no maintainer direction (only dosubot comments, all of which guess wrong). Fix is ~15 lines in one file plus a spec in an existing, well-mocked spec file. Not reproduced live (read-only stage); one small design choice remains (trimmed menu vs null menu), so I chose the variant with the least behaviour change. |
| 9942 | triage | no-go | low | The report cannot be root-caused from code, and the only proposed cause on the thread (dosubot: missing popstate listener) is wrong. A browser fires hashchange as well as popstate on Back between pushState entries; I confirmed this in Chromium 150 only, and the reporter uses Firefox, which I did not test. The handler is unchanged since before v0.103.0, there are no duplicate reports, and the reporter's URL shape is unknown. I found one code path that matches the symptoms (the "/#root" substring guard rejects some URL shapes), but the link to this report is speculation. I did not run Trilium in either shell, so reproduction is untested: on desktop, entrypoints.ts:120-128 sends back/forward through window.electronApi.navigation instead of window.history, and the reporter uses server access from a browser. The defect, if any, is on the browser path. A PR now would be a guess; the comment draft asks the reporter for the missing fact. |
| navguard | final | agent-failed | high | The guard in parseNavigationStateFromUrl is a substring test for '/#root' or '/#?searchString', so it rejects Trilium's own address whenever a query string or a slash-less pathname comes before the hash. Both window.location.href callers then get {}. The hashchange handler (Back/Forward) does nothing. loadTabs does not open or activate the URL's note on a fresh load or reload. It is a clear defect, still on upstream/main 1a2f27f8d3, with no overlapping PR or assignee. The fix is about 10 lines, fully unit-testable, and needs no new dependency. Caveat: this stage was read-only, so I did not run vitest or the live app; I ran the guard expression verbatim in node against the URL shapes instead. |
| navguard | final | not-ready | high | The guard in parseNavigationStateFromUrl is a substring test for '/#root' or '/#?searchString', so it rejects Trilium's own address whenever a query string or a slash-less pathname comes before the hash. Both window.location.href callers then get {}. The hashchange handler (Back/Forward) does nothing. loadTabs does not open or activate the URL's note on a fresh load or reload. It is a clear defect, still on upstream/main 1a2f27f8d3, with no overlapping PR or assignee. The fix is about 10 lines, fully unit-testable, and needs no new dependency. Caveat: this stage was read-only, so I did not run vitest or the live app; I ran the guard expression verbatim in node against the URL shapes instead. |
