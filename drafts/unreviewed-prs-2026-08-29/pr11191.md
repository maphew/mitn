<!-- DRAFT - NOT POSTED. Target: TriliumNext/Trilium PR 11191. Suggested event: COMMENT. -->

The resolve-and-store path is substantial, but the user-facing contract runs ahead of the implementation. The PR body says installed packages cannot yet be `require()`d, while Server-side imports documents working `require()`, offline execution, and lazy evaluation. Editor completions imply the same. Advise either keeping those surfaces out until loading works or labeling them as prototype-only.

Advise also splitting the unrelated geomap search, autocomplete, dialog, RawHtml, and infrastructure refactors from the package-storage spike so each behavior can be validated independently.

_amp-gpt-5.6-sol-high on behalf of maphew_
