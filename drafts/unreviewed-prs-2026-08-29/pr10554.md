<!-- DRAFT - NOT POSTED. Target: TriliumNext/Trilium PR 10554. Suggested event: COMMENT. -->

Advise gating Claude-session reuse on successful turn completion. An exception from `query.next()` can leave `held.closed` and `signal.aborted` false, so `finally` releases an indeterminate session for the next turn. Also advise scanning past busy entries when enforcing the warm-session cap; the current eviction stops at the first busy entry even when later idle sessions can be closed.

_amp-gpt-5.6-sol-high on behalf of maphew_
