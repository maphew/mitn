<!-- DRAFT - NOT POSTED. Target: TriliumNext/Trilium PR 10439. Suggested event: COMMENT. -->

The native interception path needs a per-navigation lifecycle. `isJsReady` remains true after the first ready message, so requests during a later page reload can be delivered before the new page installs `window.__triliumNativeRequest` and are then lost. Cancellation also removes the pending task but leaves its request in `queuedRequests`, so cancelled work still runs after readiness. Advise resetting bridge state for each navigation and pruning queued requests on cancellation before device validation.

_amp-gpt-5.6-sol-high on behalf of maphew_
