<!-- DRAFT - NOT POSTED. Target: TriliumNext/Trilium PR 11232. Suggested event: REQUEST_CHANGES. -->

Advise resolving two contract regressions before merge. Scope raw string responses so the office-preview optimization does not silently change every standalone string route. Also update the spreadsheet print-height test to read the generated class or computed value. Current CI exposes both paths: the route wrapper receives an empty body instead of JSON `""`, and all four spreadsheet E2E variants derive `NaN` for declared row height.

_amp-gpt-5.6-sol-high on behalf of maphew_
