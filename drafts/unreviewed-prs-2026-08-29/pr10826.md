<!-- DRAFT - NOT POSTED. Target: TriliumNext/Trilium PR 10826. Suggested event: COMMENT. -->

`TRILIUM_DEV_PORT` is converted with `Number()` and propagated without validation. Advise rejecting non-integer and out-of-range values at startup so configuration errors fail at the harness boundary instead of later in server initialization. The PR also combines harness initialization, timeout changes, and Split.js configuration; advise considering separate changes if maintainers do not need them as one unit.

_amp-gpt-5.6-sol-high on behalf of maphew_
