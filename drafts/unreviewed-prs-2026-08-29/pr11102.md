<!-- DRAFT - NOT POSTED. Target: TriliumNext/Trilium PR 11102. Suggested event: COMMENT. -->

The pure wheel helpers have focused coverage, but the stateful handler in `tab_row.ts` remains uncovered and `codecov/patch` is red. Advise covering command dispatch, propagation, and coalescing state at the widget-handler boundary. The issue describes an optional browser preference, while this implementation is always on, so the default interaction also needs an explicit product decision.

_amp-gpt-5.6-sol-high on behalf of maphew_
