<!-- DRAFT - NOT POSTED. Target: TriliumNext/Trilium PR 11126. Suggested event: COMMENT. -->

Advise deciding compatibility for existing custom icon manifests before making icon keys ASCII-only. `generateCss()` now skips every key outside alphanumerics, dashes, and underscores, while the current guide says only that keys should match the font's class names. An existing CSS-usable escaped key can therefore disappear after upgrade. Escaping selector identifiers, or documenting and testing this as an intentional migration, would avoid a silent compatibility break. The other stylesheet-boundary and CSS-string escaping changes address the traced injection paths.

_amp-gpt-5.6-sol-high on behalf of maphew_
