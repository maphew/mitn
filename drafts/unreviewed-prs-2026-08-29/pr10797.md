<!-- DRAFT - NOT POSTED. Target: TriliumNext/Trilium PR 10797. Suggested event: COMMENT. -->

The reload path prunes removed selected IDs but can leave `selectionAnchor.current` pointing to an attribute no longer present in `selectableIds`. The next Shift-click cannot resolve the anchor and silently skips range selection. Advise clearing or replacing the anchor in `pruneSelection()` and covering attribute removal followed by Shift-selection.

_amp-gpt-5.6-sol-high on behalf of maphew_
