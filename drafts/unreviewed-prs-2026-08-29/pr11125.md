<!-- DRAFT - NOT POSTED. Target: TriliumNext/Trilium PR 11125. Suggested event: REQUEST_CHANGES. -->

Advise limiting HTML skeleton normalization to HTML text notes. The handler calls `isVisuallyEmptyTextContent()` for every string-content note, so a code or plain-text note whose literal visible source is `<p><br></p>` is classified as empty and can be overwritten when a template relation is added. Non-HTML string content should retain the previous empty or whitespace check. The CKEditor emptied-note regression is otherwise covered directly.

_amp-gpt-5.6-sol-high on behalf of maphew_
