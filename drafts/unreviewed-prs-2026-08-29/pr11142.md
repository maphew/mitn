<!-- DRAFT - NOT POSTED. Target: TriliumNext/Trilium PR 11142. Suggested event: REQUEST_CHANGES. -->

Advise making `resetMemoizedNoteProperties()` type-safe while preserving the unloaded state. Its four `undefined` assignments currently fail TypeScript with TS2322. Resetting them to `null` would not reproduce the same state because `null` participates in numeric comparison, so the fixture needs a typed cold-state boundary rather than a value substitution. The production changes otherwise follow the two reported failure paths and have direct regression coverage.

_amp-gpt-5.6-sol-high on behalf of maphew_
