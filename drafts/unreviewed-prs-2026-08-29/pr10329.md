<!-- DRAFT - NOT POSTED. Target: TriliumNext/Trilium PR 10329. Suggested event: COMMENT. -->

The POC demonstrates the inverse-tree idea, but construction distinguishes clones by `branchId` while rendered roots and children use `noteId` as the React key. Same-note sibling clones can collide during reconciliation. Advise carrying `branchId` into `InverseTreeNode` and using it for both keys. Automated coverage remains the main promotion step already identified by the author.

_amp-gpt-5.6-sol-high on behalf of maphew_
