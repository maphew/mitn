<!-- DRAFT - NOT POSTED. Target: TriliumNext/Trilium PR 11101. Suggested event: REQUEST_CHANGES. -->

The removal marker closes the race only inside the renderer that handled the delete. `removedColumnsByNote` is module-local, so another browser client or desktop renderer can receive the attachment update before the definition update without a marker. It can resolve the stale definition, write the removed column back to `board.json`, and then sync that column into the definition again. Advise carrying removal intent across clients, or ordering and modeling the writes so no renderer can treat the intermediate state as a new column. A two-renderer ordering test should cover this sequence.

_amp-gpt-5.6-sol-high on behalf of maphew_
